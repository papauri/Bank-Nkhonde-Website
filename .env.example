# Copy this file to .env and fill in real local values.
# This file is tracked by git — it must never contain a real credential.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=bank_nkhonde
DB_USER=root
DB_PASSWORD=changeme

SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
